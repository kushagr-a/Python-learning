# def is keyword to define a function
# printOrder is the name of the function
# pass used to avoid indentation error, it does nothing


def printOrder(name, chai_type):  # here passing the parameters in () -> this blocks
    print(f"Order received for {name} with {chai_type} chai.")



printOrder("kushagra", "masala")  # here we are passing the arguments in () -> this blocks
printOrder("Alice", "ginger") 